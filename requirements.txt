# Core training / eval dependencies
torch==2.11.0
transformers==5.8.0
datasets==4.8.5
accelerate
wandb
trackio
tiktoken
sentencepiece
protobuf
lm-eval
tqdm
pypdf
certifi

# TorchTitan-backed scripts
torchtitan==0.2.2

# Optional data collection helpers
yt-dlp
youtube-transcript-api

# Optional CUDA accelerators
# Install these only in a compatible Linux/CUDA environment.
# Blackwell/Hopper training stack used by the TorchTitan scripts. Install the
# CUDA-matched flash-attn-4 extra as documented in setup.sh.
flash-attn-4==4.0.0b12
liger-kernel==0.8.0
# Kimi Delta Attention layers (Triton chunk_kda) used by baseline_llama_KDA.py
flash-linear-attention==0.5.2
